#!/bin/sh
set -eu

repo_dir=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
cd "$repo_dir"

cargo metadata --no-deps --format-version 1 >/dev/null
if [ ! -x .pixi/envs/ennx/bin/python ]; then
    echo "missing Python environment; run ./ennx build first" >&2
    exit 1
fi
